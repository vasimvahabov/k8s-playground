#!/bin/bash

set +ex

# current_dir=`echo ${PWD##*/}`
# current_ns=`kubectl config view --minify -o=jsonpath='{.contexts[0].context.namespace}'`

# echo $current_dir
# echo $current_ns

# if [ $current_dir!=$current_ns ]; then
#     echo "Error : current namespace is $current_ns but $current_dir expected.."
#     exit 1
# else
#     kubectl apply

# fi

# exit 0
